class PlayerState {
    constructor(x = 0, y = 0) {
        this.x = x;
        this.y = y;
        // You can add more properties here, e.g. velocity, health, etc.
    }
}

module.exports = PlayerState;